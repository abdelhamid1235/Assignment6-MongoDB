Assihnment_8 :-
Commands MomgoShell:-
1- mongosh
2- use BookStoreDB
Create an explicit collection named “books” with a validation rule to ensure that each document has a non-empty “title” field.
3- db.createCollection('Books' , {validator:{ title: {
      $exists: true,
      $type: "string",
     $ne: ""
    }}})
4- db.Books.insertOne({title:"Book_1"})

Create an implicit collection by inserting data directly into a new collection named
“authors”. (0.5 Grade)

5- db.authors.insertOne({name:"Author1" , nationality:"British"})

Create a capped collection named “logs” with a size limit of 1MB. (0.5 Grade)

6- db.createCollection('logs' , {capped:true , size : 1048576 ,max:2})
7- db.logs.insertOne({title:"tyy"})

Create an index on the books collection for the title field. (0.5 Grade)
8- db.Books.createIndex({title:1})

5.
Insert one document into the books collection. (0.5 Grade)

db.Books.insertOne({title:"Book1" , author:"Ali" , year:1937 , genres:["Fantasty " , "Adventure"]})

6.
Insert multiple documents into the books collection with at least three records. (0.5 Grade)v

db.Books.insertMany([
    { "title": "Future", "author": "Ali2", "year": 1937, "genres": ["Fantasty", "Adventure"] },
    { "title": "To Kill a Mockingbird", "author": "Ali2", "year": 1937, "genres": ["Fantasty", "Adventure"] },
    { "title": "Bravo New World", "author": "Ali2", "year": 1937, "genres": ["Fantasty", "Adventure"] },
    { "title": "Book6", "author": "Ali2", "year": 1937, "genres": ["Fantasty", "Adventure"] }
]);

7.Insert a new log into the logs collection. (0.5 Grade)

db.logs.insertOne({"book_id":"6978f9a890e4ee2e3b17df10" , "action" :"borrowed"})

8.
Update the book with title “Future” change the year to be 2022.
db.Books.updateOne({title:"Future" },{$set:{year : 2022}})

9.
Find a Book with title “Brave New World”. (0.5 Grade)
db.Books.find({title:"Brave New World"})

10.
Find all books published between 1990 and 2010. (0.5 Grade)
db.Books.find({year:{ $gt: 1900, $lt: 2010 }})


 11.
Find books where the genre includes "Science Fiction".(0.5 Grade)

db.Books.find({genres:"Science Fiction"})

12.
 Skip the first two books, limit the results to the next three, sorted by year in descending order. (0.5 Grade)

db.Books.find({}).skip(2).limit(3).sort({year:-1})


13.
Find books where the year field stored as an integer. (0.5 Grade)

db.Books.find({year:{$type:"int"}})

14.
 Find all books where the genres field does not include any of the genres "Horror" or "Science Fiction". (0.5 Grade)

db.Books.find({genres: { $nin: ["Horror", "Science Fiction"] }})

15.
 Delete all books published before 2000. (0.5 Grade)

db.Books.deleteMany({ year : {$lt : 2000}})

16.
 Using aggregation Functions, Filter books published after 2000 and sort them by year descending. (0.5 Grade)

 db.Books.aggregate(
         [
             {
                 $match:{year: {$gt:2000 }}

             },
             {
                 $sort:{year:-1}

             }
         ]
     )

17.Using aggregation functions,
Find all books published after the year 2000. For each matching book, 
show only the title, author, and year fields. (0.5 Grade)

db.Books.aggregate(
         [
             {
                 $match:{year: {$gt:2000 }}

             },
             {
                 $project:{
                     title:1 , author:1 , year:1 , _id:0

                 }
             },
             {
                 $sort:{year:-1}

             }
         ]
     )

18.
Using aggregation functions,break an array of genres into separate documents. (0.5 Grade)


db.Books.aggregate([
         {
             $unwind: "$genres"
         },
         {
             $project:{
                 title:1 , genres:1 , _id:0
             }
         }
     ])

19.
Using aggregation functions, Join the books collection with the logs collection. (1 Grade)

 db.Books.aggregate([

         {$lookup: {
                 from: "books",
                 localField: "book_id",
                 foreignField: "_id",
                 as: "bookDetails"
             },
         },

         {
                 $project:{
                     _id:0 , book_id:0

                 }
         }

     ])